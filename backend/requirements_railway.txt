# Lightweight requirements for Railway deployment
# Using CPU-only versions to reduce memory usage

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# PyTorch CPU version for Railway
--find-links https://download.pytorch.org/whl/torch_stable.html
torch==2.0.1+cpu
torchvision==0.15.2+cpu

# Transformers and dependencies
transformers>=4.30.0
accelerate>=0.20.0

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# 3D processing (lightweight)
trimesh>=3.21.0

# Configuration and utilities
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
httpx>=0.24.0
psutil>=5.9.0

# Memory optimization
numba>=0.57.0